import React from 'react';
import { View } from 'react-native';

import styles from '../../styles';

const BlankShortcut = () => <View style={styles.shortcut.blankView} />;

export default BlankShortcut;
